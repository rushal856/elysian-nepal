'use client';
import {useEffect,useState} from 'react'; import {Brand} from '@/components/Brand'; import {PRODUCT,formatNPR} from '@/lib/product';
type Order={productName:string;quantity:number;totalPrice:number;orderId:string};
export default function ThankYou(){const [order,setOrder]=useState<Order|null>(null);useEffect(()=>{const raw=sessionStorage.getItem('order');if(raw)setOrder(JSON.parse(raw))},[]);return <main className="checkout-page"><div className="shell"><div className="checkout-header"><Brand/></div><div className="thank"><div className="card"><div className="success">✓</div><div className="eyebrow">Order received</div><h1 className="serif">Thank you for your order!</h1><p className="muted">Our sales representative will call you soon to confirm your order.</p><div style={{background:'#f7f1e8',borderRadius:14,padding:18,textAlign:'left',margin:'26px 0'}}><div className="summary-line"><span>Order ID</span><strong>{order?.orderId||'—'}</strong></div><div className="summary-line"><span>Product</span><strong>{order?.productName||PRODUCT.name}</strong></div><div className="summary-line"><span>Quantity</span><strong>{order?.quantity||1}</strong></div><div className="summary-line"><span>Total price</span><strong>{formatNPR(order?.totalPrice||PRODUCT.price)}</strong></div><div className="summary-line" style={{border:0}}><span>Payment</span><strong>Cash On Delivery</strong></div></div><a className="btn gold" href="/">Back to Home</a></div></div></div></main>}
