'use client';
import {useState} from 'react'; import {BuyButton} from './BuyButton'; import {ProductGallery} from './ProductGallery'; import {QuantityPicker} from './QuantityPicker'; import {PRODUCT,formatNPR} from '@/lib/product';
export function Showcase(){const [quantity,setQuantity]=useState(1);return <section className="section" id="shop"><div className="shell showcase"><ProductGallery/><div><div className="eyebrow">The everyday statement</div><h2 className="serif">Chasma</h2><p className="muted">Bold rectangular sunglasses with a rich dark tint and a polished oversized silhouette—made to instantly elevate everyday looks.</p><div className="benefits-mini"><div>✦ Stylish oversized frame</div><div>☀ Helps reduce glare in bright sunlight</div><div>✦ Travel, parties & everyday-ready</div></div><p><span className="price-old">{formatNPR(PRODUCT.originalPrice)}</span> <strong className="gold-text" style={{fontSize:'1.55rem',marginLeft:10}}>{formatNPR(PRODUCT.price)}</strong></p><div className="buy-box card"><div className="total-row"><span>Quantity</span><QuantityPicker quantity={quantity} setQuantity={setQuantity}/></div><div className="total-row"><strong>Your total</strong><strong style={{fontSize:'1.25rem'}}>{formatNPR(PRODUCT.price*quantity)}</strong></div><BuyButton quantity={quantity} className="btn gold" >Buy Now — COD</BuyButton></div></div></div></section>}
